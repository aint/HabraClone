<b:beans xmlns="http://www.springframework.org/schema/security"
         xmlns:b="http://www.springframework.org/schema/beans"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
						http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

    <http pattern="/admin/**" auto-config="true" authentication-manager-ref="adminAuthManager">
        <form-login login-page="/admin/login" />
        <logout logout-url="/logout" invalidate-session="true" />
    </http>

    <authentication-manager alias="adminAuthManager">
        <authentication-provider ref="customAuthenticationProvider" />
    </authentication-manager>


    <http pattern="/api/**" entry-point-ref="restAuthenticationEntryPoint" create-session="stateless" >
        <intercept-url pattern="/api/articles/**" access="permitAll"/>
        <intercept-url pattern="/api/users/**" access="permitAll"/>

        <intercept-url pattern="/api/authenticate" access="permitAll"/>

        <intercept-url pattern="/api/admin/**" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/api/comments/add" access="isAuthenticated()"/>

        <access-denied-handler ref="restAccessDeniedHandler"/>
        <custom-filter ref="tokenAuthenticationFilter" position="FORM_LOGIN_FILTER"   />

        <logout />
        <csrf disabled="true" />
    </http>

    <http auto-config="true" authentication-manager-ref="authenticationManager">
        <intercept-url pattern="/articles/add" access="isAuthenticated()" />
        <intercept-url pattern="/comments/add" access="isAuthenticated()" />
        <intercept-url pattern="/articles/**/vote/plus" access="isAuthenticated()" />
        <intercept-url pattern="/articles/**/vote/minus" access="isAuthenticated()" />

        <intercept-url pattern="/users/**/ban" access="hasRole('ROLE_ADMIN')" />
        <intercept-url pattern="/users/**/unban" access="hasRole('ROLE_ADMIN')" />
        <intercept-url pattern="/articles/**/delete" access="hasRole('ROLE_ADMIN')" />
        <intercept-url pattern="/comments/**/delete" access="hasRole('ROLE_ADMIN')" />

        <form-login login-page="/login" authentication-success-handler-ref="successfulAuthenticationHandler"/>
        <logout logout-success-url="/" invalidate-session="true" delete-cookies="remember-me" />

        <remember-me key="somehashcode" token-validity-seconds="86400" user-service-ref="userTransactionalService" />

        <csrf/>
    </http>

    <authentication-manager id="authenticationManager">
        <authentication-provider user-service-ref="userTransactionalService" >
            <password-encoder ref="encoder" />
        </authentication-provider>
    </authentication-manager>

    <b:bean id="encoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder">
        <b:constructor-arg name="strength" value="11" />
    </b:bean>

</b:beans>
